document.addEventListener("DOMContentLoaded", function() {
    const audioPlayer = document.getElementById('audio-player');
    const bgVideo = document.getElementById('bg-video');

    // Play the background video when the audio starts playing
    audioPlayer.addEventListener('play', function() {
        bgVideo.play();
    });

    // Pause the background video when the audio is paused or ended
    audioPlayer.addEventListener('pause', function() {
        bgVideo.pause();
    });

    audioPlayer.addEventListener('ended', function() {
        bgVideo.pause();
        bgVideo.currentTime = 0;
    });

    
});

document.addEventListener("DOMContentLoaded", function() {
    const audioPlayer2 = document.getElementById('audio-player2');
    const bgVideo2 = document.getElementById('bg-video2');

    // Play the background video when the audio starts playing
    audioPlayer2.addEventListener('play', function() {
        bgVideo2.play();
    });

    // Pause the background video when the audio is paused or ended
    audioPlayer2.addEventListener('pause', function() {
        bgVideo2.pause();
    });

    audioPlayer2.addEventListener('ended', function() {
        bgVideo2.pause();
        bgVideo2.currentTime = 0;
    });

    
});



document.addEventListener("DOMContentLoaded", function() {
    const audioPlayer2 = document.getElementById('audio-player4');
    const bgVideo2 = document.getElementById('bg-video4');

    // Play the background video when the audio starts playing
    audioPlayer2.addEventListener('play', function() {
        bgVideo2.play();
    });

    // Pause the background video when the audio is paused or ended
    audioPlayer2.addEventListener('pause', function() {
        bgVideo2.pause();
    });

    audioPlayer2.addEventListener('ended', function() {
        bgVideo2.pause();
        bgVideo2.currentTime = 0;
    });

    
});

document.addEventListener("DOMContentLoaded", function() {
    const audioPlayer2 = document.getElementById('audio-player5');
    const bgVideo2 = document.getElementById('bg-video5');

    // Play the background video when the audio starts playing
    audioPlayer2.addEventListener('play', function() {
        bgVideo2.play();
    });

    // Pause the background video when the audio is paused or ended
    audioPlayer2.addEventListener('pause', function() {
        bgVideo2.pause();
    });

    audioPlayer2.addEventListener('ended', function() {
        bgVideo2.pause();
        bgVideo2.currentTime = 0;
    });

    
});


document.addEventListener("DOMContentLoaded", function() {
    const audioPlayer2 = document.getElementById('audio-player6');
    const bgVideo2 = document.getElementById('bg-video6');

    // Play the background video when the audio starts playing
    audioPlayer2.addEventListener('play', function() {
        bgVideo2.play();
    });

    // Pause the background video when the audio is paused or ended
    audioPlayer2.addEventListener('pause', function() {
        bgVideo2.pause();
    });

    audioPlayer2.addEventListener('ended', function() {
        bgVideo2.pause();
        bgVideo2.currentTime = 0;
    });

    
});


